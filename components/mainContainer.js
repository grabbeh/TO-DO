const Container = ({ children }) => (
  <div className='font:sans mb-5 flex justify-center'>
    <div className='h-full relative w-full md:w-2/5'>{children}</div>
  </div>
)

export default Container
